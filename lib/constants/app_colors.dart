import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF4461F2);
  static const Color secondary = Color(0xFF5E7BFB);
  static const Color accent = Color(0xFF7993FF); // Added accent color

  // Text colors
  static const Color text = Color(0xFF303030);
  static const Color textLight = Color(0xFF707070);

  // Background colors
  static const Color background = Color(0xFFF8F8F8);
  static const Color cardBackground = Color(0xFFFFFFFF);

  // Status colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFF9800);
  static const Color info = Color(0xFF2196F3);

  // Additional colors for UI elements
  static const Color divider = Color(0xFFE0E0E0);
  static const Color disabled = Color(0xFFBDBDBD);
  static const Color highlight = Color(0xFFE3F2FD);
}
